service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
    match /images/{randomId}/{file} {
      allow write: if resource == null;
    }
  }
}
